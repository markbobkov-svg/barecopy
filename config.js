/* ============================================================
   Barecopy — YOUR settings. This is the ONLY file you edit by hand.
   It is never regenerated, so your values survive every code update.

   Fill in the two values below from your Polar dashboard, then
   commit this file. After that: download a new build, commit, done —
   no manual editing ever again.

   Where to find them:
     • BARECOPY_ORG_ID   → Polar → Settings → General → Organization ID
     • BARECOPY_CHECKOUT → Polar → Products → Barecopy Pro →
                            Checkout Links → your buy.polar.sh/... link
   ============================================================ */

window.BARECOPY_ORG_ID  = "TODO_POLAR_ORG_ID";
window.BARECOPY_CHECKOUT = "TODO_POLAR_CHECKOUT_URL";
